Metadata-Version: 2.4
Name: pachong
Version: 0.1.0
Summary: Dynamic website crawler MVP built with Playwright
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: markdownify>=1.1.0
Requires-Dist: playwright>=1.53.0
Requires-Dist: trafilatura>=2.0.0
Requires-Dist: pydantic>=2.11.0
Requires-Dist: PyYAML>=6.0.2
Requires-Dist: selectolax>=0.3.26
Requires-Dist: typer>=0.16.0

# Pachong

一个面向动态网站的爬虫工具 `MVP`。当前版本先聚焦最小闭环：

- 使用 `Playwright` 抓取动态渲染后的页面
- 保存原始 HTML 和页面截图
- 使用 `Trafilatura` 提取正文并输出 Markdown
- 输出结构化的任务结果 JSON

## 安装

```bash
pip install -e .
playwright install chromium
```

如果刚更新过代码和依赖定义，先重新执行一次：

```bash
pip install -e .
```

## 快速开始

```bash
pachong crawl https://example.com/article/123
```

默认输出目录：

- `data/html/`
- `data/markdown/`
- `data/json/`
- `data/screenshots/`

## 使用站点配置

```bash
pachong crawl https://example.com/article/123 --config configs/sites/example_article.yaml
```

文档站这类“页面框架先出现、正文后注入”的站点，建议显式传配置。例如：

```bash
pachong crawl "https://doc.jjjshop.net/multi?category_id=10026&document_id=116" --config configs/sites/jjjshop_doc.yaml
```

如果要把当前左侧栏目下的所有页面批量抓取下来，可以用：

```bash
pachong batch "https://doc.jjjshop.net/multi?category_id=10026&document_id=116"
```

默认会输出到：

- `data/batch/jjjshop_doc/category_10026/markdown/`
- `data/batch/jjjshop_doc/category_10026/html/`
- `data/batch/jjjshop_doc/category_10026/toc.json`
- `data/batch/jjjshop_doc/category_10026/目录总览.md`

不同 `category_id` 会自动输出到不同子目录，避免互相覆盖。

## 当前能力

- 单 URL 抓取
- 基于配置的等待策略
- 支持等待正文区域出现并填充文本
- 自动保存 HTML、Markdown、JSON、截图
- 支持标题和正文 selector 提取
- 在 selector 提取失败时回退到 `Trafilatura`

## 后续扩展方向

- 批量任务队列
- SQLite 任务状态管理
- 网络接口监听
- 登录态复用
- 代理与重试策略
